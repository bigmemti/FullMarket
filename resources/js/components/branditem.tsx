// This component is no longer needed as the functionality has been integrated directly into the index.tsx file
// The component was removed to avoid duplicate mapping over the data and to simplify the HTML structure
// The responsive design is now handled with Tailwind's responsive classes in a single component